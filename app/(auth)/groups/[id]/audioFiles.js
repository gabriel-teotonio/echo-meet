// /app/groups/[id]/participants.js
import { View, Text } from 'react-native';
// import { useSearchParams } from 'expo-router';

export default function Participants() {
//   const { id } = useSearchParams();

  return (
    <View>
        <Text>audioFiles</Text>
    </View>
  );
}
