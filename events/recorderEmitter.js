import { NativeEventEmitter } from "react-native";

// Crie uma instância do EventEmitter
const audioEmitter = new NativeEventEmitter();

// Exporte para que outros arquivos possam usar
export default audioEmitter;
